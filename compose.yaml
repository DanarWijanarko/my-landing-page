services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-landing-page
    container_name: my-landing-page
    restart: unless-stopped
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=production
      - PORT=3000